# <Project name> — Claude context

> Project-level CLAUDE.md. Merges with the mg-harness CLAUDE.md.
> Replace placeholders below before committing.

## What this project is

<2-3 sentences. Domain, role, key consumers.>

## Architecture quick reference

- **Language**: <e.g. TypeScript / Node 22>
- **Framework**: <e.g. Fastify / Next.js / Rails>
- **Database**: <e.g. Postgres via Prisma>
- **Tests**: <e.g. Vitest / pytest>
- **Deployment**: <e.g. AWS ECS via GitHub Actions>

## Where things live

- `src/` — application code
- `test/` — tests
- ...

## Project-specific conventions

<Anything specific to this project that the harness's defaults don't cover.>

## Build & test commands

- `<command>` — local dev server
- `<command>` — full test suite
- `<command>` — lint + type-check

## Known foot-guns

<Document the things that have bitten contributors in the past. Future Claude reads this to avoid the same mistakes.>

- <Foot-gun 1>
- <Foot-gun 2>
